package com.acme.banking.api.dto;

import com.acme.banking.core.model.Card;
import com.acme.banking.core.model.CardStatus;
import jakarta.annotation.Generated;
import java.lang.Long;
import java.lang.String;
import java.math.BigDecimal;
import java.time.LocalDate;

@Generated(
        value = "io.hexaglue.plugin.rest",
        comments = "Generated by HexaGlue REST Plugin"
)
public record CardResponse(Long id, String cardNumber, LocalDate expiryDate, String cvv,
        Long accountId, CardStatus status, BigDecimal dailyLimitAmount, String dailyLimitCurrency) {
    public static CardResponse from(Card source) {
        return new CardResponse(
                        source.id() != null ? source.id().value() : null,
                        source.cardNumber(),
                        source.expiryDate(),
                        source.cvv(),
                        source.accountId() != null ? source.accountId().value() : null,
                        source.status(),
                        source.dailyLimit() != null ? source.dailyLimit().amount() : null,
                        source.dailyLimit() != null ? source.dailyLimit().currency() : null);
    }
}
