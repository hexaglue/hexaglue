package com.acme.banking.api.controller;

import com.acme.banking.api.dto.InitiateTransferRequest;
import com.acme.banking.api.dto.TransferResponse;
import com.acme.banking.core.model.AccountId;
import com.acme.banking.core.model.Money;
import com.acme.banking.core.model.TransferId;
import com.acme.banking.core.port.in.TransferUseCases;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.annotation.Generated;
import jakarta.validation.Valid;
import java.lang.Long;
import java.lang.Object;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@Generated(
        value = "io.hexaglue.plugin.rest",
        comments = "Generated by HexaGlue REST Plugin"
)
@RestController
@RequestMapping("/api/transfers")
@Tag(
        name = "Transfers",
        description = "Transfer management operations"
)
public class TransferController {
    private final TransferUseCases transferUseCases;

    public TransferController(TransferUseCases transferUseCases) {
        this.transferUseCases = transferUseCases;
    }

    @Operation(
            summary = "Get transfer"
    )
    @ApiResponse(
            responseCode = "200",
            description = "Get transfer completed successfully"
    )
    @GetMapping("/{id}")
    public ResponseEntity<Object> getTransfer(@PathVariable Long id) {
        var result = transferUseCases.getTransfer(
                        new TransferId(id));
        return ResponseEntity.ok(TransferResponse.from(result));
    }

    @Operation(
            summary = "Initiate transfer"
    )
    @ApiResponse(
            responseCode = "201",
            description = "Initiate transfer completed successfully"
    )
    @PostMapping
    public ResponseEntity<Object> initiateTransfer(
            @Valid @RequestBody InitiateTransferRequest request) {
        var result = transferUseCases.initiateTransfer(
                        new AccountId(request.fromAccountId()),
                        new AccountId(request.toAccountId()),
                        Money.of(request.amount(), request.currency()));
        return ResponseEntity.ok(TransferResponse.from(result));
    }

    @Operation(
            summary = "Get transfers by account"
    )
    @ApiResponse(
            responseCode = "200",
            description = "Get transfers by account completed successfully"
    )
    @GetMapping("/by-account/{accountId}")
    public ResponseEntity<Object> getTransfersByAccount(@PathVariable AccountId accountId) {
        var result = transferUseCases.getTransfersByAccount(
                        new AccountId(id));
        return ResponseEntity.ok(result);
    }
}
