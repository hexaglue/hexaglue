package com.coffeeshop.domain.order;

/**
 * Order lifecycle status.
 * Expected classification: VALUE_OBJECT (enum)
 */
public enum OrderStatus {
    PENDING,
    SUBMITTED,
    COMPLETED,
    CANCELLED
}
