package com.coffeeshop.domain.order;

/**
 * Where the order will be consumed.
 * Expected classification: VALUE_OBJECT (enum, immutable, no identity)
 */
public enum Location {
    IN_STORE,
    TAKE_AWAY
}
