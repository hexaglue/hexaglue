# HexaGlue configuration for tutorial-audit
# This example uses default values - shows a clean audit pass

plugins:
  io.hexaglue.plugin.audit:
    # Default settings - strict mode
    errorOnBlocker: true
    errorOnCritical: true
