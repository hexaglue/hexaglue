package com.example.ecommerce.domain.validation;

import com.example.ecommerce.domain.model.Inventory;

/**
 * Validator for Inventory.
 */
public class InventoryValidator {
    public ValidationResult validate(Inventory entity) {
        ValidationResult result = new ValidationResult();
        // Add validations
        return result;
    }
}
