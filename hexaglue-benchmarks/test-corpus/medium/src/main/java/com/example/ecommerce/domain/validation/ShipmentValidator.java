package com.example.ecommerce.domain.validation;

import com.example.ecommerce.domain.model.Shipment;

/**
 * Validator for Shipment.
 */
public class ShipmentValidator {
    public ValidationResult validate(Shipment entity) {
        ValidationResult result = new ValidationResult();
        // Add validations
        return result;
    }
}
